# TODO

Актуальный список на фоне MVP + модуля коммуникаций. Обновлять при закрытии пунктов.

## A) Сделать в ближайшее время

| # | Задача | Зачем |
|---|--------|--------|
| 1 | Закоммитить / задеплоить актуальный `dev`↔`main` и код communications на обе VM | Иначе прод расходится с локальным |
| 2 | На Telegram-VM: timer daily report + `send_at`/`chat_id` в config; проверка `--force` / минута `send_at` | Стабильный дайджест |
| 3 | systemd unit для Telegram (`wvs-telegram.service`) | Сейчас часто nohup; рестарты хрупкие |
| 4 | Маршрутизация `/start faq` (и др. payload) → экран FAQ / нужный экран | Deep link из шаблона `faq_deeplink` станет полезным |
| 5 | Inline-кнопка в рассылке («Открыть FAQ») без обязательного `/start` | Надёжнее deep link для уже пишущих боту |
| 6 | Убедиться, что `scripts/deploy_prod.sh` копирует лендинг + рестартит нужные unit’ы | Меньше ручных шагов |
| 7 | Документировать на Telegram-VM: миграции SQL только на основной Postgres | Частая ошибка с `psql` на сокет |

## B) Когда вырастет нагрузка / аудитория

| # | Задача | Зачем |
|---|--------|--------|
| 1 | Батчинг и тюнинг rate limit рассылок (сейчас ~2 с между сообщениями; API limits) | Длинные кампании |
| 2 | Очередь/воркер исходящих коммуникаций (не только ручной CLI) | Не держать ручной прогон часами |
| 3 | Мониторинг: DAU, воронка анкеты, доля `analytics_error`, failed communications | Операционка |
| 4 | Алерты в stats-чат при всплеске ошибок / падении бота | Быстрая реакция |
| 5 | Индексы/партиции на `events` при росте объёма; архивация старых событий | Производительность SQL |
| 6 | Кэш `country_data` / reference в памяти процесса | Меньше нагрузки на Postgres при find_country |
| 7 | Горизонталь: несколько воркеров Telegram / отдельный read-replica для аналитики | Тысячи одновременных пользователей (цель из task.md) |
| 8 | CI (pytest на PR) + опционально smoke после deploy | Регрессии до прода |
| 9 | Альтернативный logger (SQLite) для офлайн/демо без Postgres | Из task.md |
| 10 | Повторные коммуникации «только недоставленным» (новый `template_id` + фильтр по `status`) | До thrёть до блокировавших бота |

## Уже сделано недавно (не трогать без нужды)

- Методология индексов: «не знаю» вне суммы; warn ≥5.
- Daily audience report + `send_at` в config.
- Рассылки: `wvs.communications`, сегменты, dry-run, stub + `faq_deeplink`.
- Фикс `find_nearest_country(..., answer_store, ...)`.
- Telegram: registration после confirm; починка numeric `user_name`.
