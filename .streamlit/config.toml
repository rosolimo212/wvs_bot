# Продакшен: слушать все интерфейсы (не только localhost).
# Порт 8502 — на VM 8501 занят template-streamlit.service.

[server]
address = "0.0.0.0"
port = 8502
headless = true

[browser]
gatherUsageStats = false
# На VM: раскомментируйте serverAddress, чтобы в консоли печатался публичный URL
# (Streamlit сам его не угадывает, если checkip.amazonaws.com недоступен).
# serverAddress = "45.132.18.2"
